apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arr-lts
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/rrumana/k8s-homelab
    targetRevision: main
    path: apps/base/arr-lts
  destination:
    server: https://kubernetes.default.svc
    namespace: arr-lts
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true