apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-certificate
  namespace: test
spec:
  secretName: test-cert-tls
  dnsNames:
  - test.k8s.rcrumana.xyz
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer