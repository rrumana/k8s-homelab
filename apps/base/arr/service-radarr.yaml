apiVersion: v1
kind: Service
metadata:
  name: radarr
  labels: { app: arr-stack }
spec:
  type: ClusterIP
  selector: { app: arr-stack }
  ports:
    - name: http
      port: 80
      targetPort: 7878
      protocol: TCP