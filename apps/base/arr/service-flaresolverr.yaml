apiVersion: v1
kind: Service
metadata:
  name: flaresolverr
  labels: { app: arr-stack }
spec:
  type: ClusterIP
  selector: { app: arr-stack }
  ports:
    - name: http
      port: 80
      targetPort: 8191
      protocol: TCP