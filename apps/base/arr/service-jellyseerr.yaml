apiVersion: v1
kind: Service
metadata:
  name: jellyseerr
  labels: { app: arr-stack }
spec:
  type: ClusterIP
  selector: { app: arr-stack }
  ports:
    - name: http
      port: 80
      targetPort: 5055
      protocol: TCP
