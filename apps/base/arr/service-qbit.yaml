apiVersion: v1
kind: Service
metadata:
  name: qbit
  labels: { app: arr-stack }
spec:
  type: ClusterIP
  selector: { app: arr-stack }
  ports:
    - name: http
      port: 80
      targetPort: 8020
      protocol: TCP