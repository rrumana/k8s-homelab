apiVersion: v2
name: homarr-db
description: Wrapper chart for Bitnami PostgreSQL-HA for Homarr (HA-ready primary with automatic failover)
type: application
version: 0.1.0

dependencies:
  - name: postgresql-ha
    # NOTE: Pin to a known-good version for your cluster. Update if needed.
    version: 14.3.6
    repository: https://charts.bitnami.com/bitnami