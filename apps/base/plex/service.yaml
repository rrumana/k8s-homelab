apiVersion: v1
kind: Service
metadata:
  name: plex
  labels:
    app: plex
spec:
  type: ClusterIP
  selector:
    app: plex
  ports:
  - name: web
    port: 32400
    targetPort: 32400
    protocol: TCP