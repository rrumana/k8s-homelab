apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: elasticsearch
resources:
  - pvc.yaml
  - pvc2.yaml
  - deployment.yaml
  - service.yaml

generatorOptions:
  disableNameSuffixHash: true